;;; env.el --- Set custom environment variables

;;; Commentary:

;; This file is only a template.  Edit this file to add custom
;; environment variables that are system dependent.  This includes
;; authentication tokens required by certain packages.  Changes to
;; this files are not meant to be in git (see .gitignore).

;;; Code:

;; Examples:
;; (setenv "MYAPP" "/home/dileban/bin/myapp")

;;; env.el ends here
